import { describe, it } from "node:test";
import assert from "node:assert";
import { BrandDialog } from "./BrandDialog";

describe("BrandDialog", () => {
  it("should be defined", () => {
    assert.ok(BrandDialog);
  });
});
