import { createCrudQueryKeys } from "../../core/crud";

/**
 * Standardized Query Key Factory for Unit queries following shared CRUD conventions
 */
export const unitKeys = createCrudQueryKeys("units");
